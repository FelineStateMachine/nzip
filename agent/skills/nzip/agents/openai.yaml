interface:
  display_name: "nzip"
  short_description: "Set up, diagnose, and use nzip safely"
  default_prompt: "Use $nzip to set up, diagnose, or publish a repository-local HTML artifact with sane defaults."
