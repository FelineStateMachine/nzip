/** Maximum number of distinct R2 blobs a manifest may reference. */
export const MAX_UNIQUE_BLOBS = 900;

/** Maximum size of one uploaded blob (50 MiB). */
export const MAX_BLOB_BYTES = 50 * 1024 * 1024;
